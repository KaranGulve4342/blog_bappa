let express,dotenv;_a3f‍.w("express",[["default",["express"],function(v){express=v}]]);_a3f‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();
const app = express();
app.use(express.json());

app.get('/', (req, res) => {
    res.send("Server is running...");
});

app.listen(PORT, () => console.log("Server is running on port `{PORT}`"));


console.log("Bappa Morya");